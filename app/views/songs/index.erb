<div class="jumbotron">
  <h1>All songs and artists <small>submitted by users!</small></h1>
</div>
<div class="container">
<% @songs.each do |song| %>
  <div class="well">
  <h3>Title: <a href="songs/<%= song.id %>"><%= song.title %><small></a> (artist: <%= song.author %>)</small></h3>
  </div>
<% end %>
</div>
<a href="/songs/new">Post a new song!</a>